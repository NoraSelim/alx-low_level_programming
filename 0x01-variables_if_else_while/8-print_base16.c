#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's about writinh alphabitics'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{int digit;
for (digit = 47; digit < 59; digit++)
{putchar(digit);
}
for (digit = 96; digit < 102; digit++)
putchar('\n');

return (0);
}
