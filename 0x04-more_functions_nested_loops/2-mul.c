#include "main.h"
/**
 * mul - Entry point
 *
 * @a:'return value'
 * @b:'return value'
 *
 * Return: return multiply (Success)
 */
int mul(int a, int b)
{
int multiply = a * b;
return (multiply);
}

