#include <stdio.h>
/**
 * mul - Entry point
 *
 * Description: 'the program's about writinh alphabitics'
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b);
{
int multiply = a * b;
return (multiply);
}

