#include "main.h"
/**
 * _abs - Entry point
 *
 * @n: 'the program's about writinh alphabitics'
 *
 * Return: Always 1 if lowercase otherwise return 0  (Success)
 */

int _abs(int n)
{
if (n < 0)
{
	n = ( -1 * n);
	return (n);
}
}

