#include "main.h"
/**
 * main - Entry point
 *
 * puts_half : 'the program's about writinh alphabitics'
 *
 * Return: Always 0 (Success)
 */
void puts_half(char *str)
{
	int i, j;

	for (i = 0; str[i] != '\0'; i++)
		;
	//try if (j = 0 ; j < i/2 ;i++)
	if (i % 2  == 0)
	{
		for (j = i / 2; str[i] != '\0'; j++)
		{
			_putchar(str[i]);
		}
	}
	else
	{
		for(n = (i - 1) /2; 
				}
				}

